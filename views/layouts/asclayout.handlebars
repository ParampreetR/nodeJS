<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>AS Collage</title>
        <link href="styles/as.css" type="text/css" rel="stylesheet">
        <script src="scripts/jquery.min.js"></script>
        {{#if style}}
            <link href="styles/{{style}}" type="text/css" rel="stylesheet">
        {{/if}}
    </head>
    <body>
        <header>
            <nav>
                <ul class="hiddenInfo">
                    <li class="liEmail">abc@ada.com</li>
                    <li class="liAddress">California, Near Highway, Green Town</li>
                    <li class="liPhone">98888888823</li>
                    <li class="liSite">www.asckhn.com</li>
                </ul>
                <div class="midNav">
                    <img src="imgs/ascollegenew.png" class="topLogo">
                </div>
                <div class="lowerNav">
                    <a href="#" class="lowerNavButtons">Home</a>
                    <a href="#" class="lowerNavButtons">About</a>
                    <a href="#" class="lowerNavButtons">Rules</a>
                    <a href="#" class="lowerNavButtons">Courses</a>
                    <a href="#" class="lowerNavButtons">Activities</a>
                    <a href="#" class="lowerNavButtons">Others</a>
                </div>
            </nav>
        </header>
        <main>
            {{{body}}}
        </main>
        <footer>

        </footer>
    </body>
</html>